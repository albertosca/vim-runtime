# Backlog

- **Rename the GitHub repo** (`albertosca/vim` → something more descriptive; the local dir is `~/.vim_runtime` and the description says "My polyglot Vim/Neovim setup"). GitHub auto-redirects old URLs after a rename, but the submodule URL in any consumer and the `install.sh` clone instructions must be updated in the same pass. (Alberto, 2026-08-13)
- **Apply the showcase standards** (global rule of 2026-08-13): bilingual README with flag list at the top (🇺🇸/🇧🇷), quality badges (CI/linter/coverage where honestly measurable), English repo description (done 2026-08-13 — the rest pending). See vim-ai-autocomplete for the reference implementation.
